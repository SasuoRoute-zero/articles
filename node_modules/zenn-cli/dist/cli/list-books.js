"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.exec = void 0;
var arg_1 = __importDefault(require("arg"));
var safe_1 = __importDefault(require("colors/safe"));
var constants_1 = require("./constants");
var books_1 = require("../utils/api/books");
function parseArgs(argv) {
    try {
        return arg_1.default({
            // Types
            '--format': String,
            '--help': Boolean,
            // Alias
            '-h': '--help',
        }, { argv: argv });
    }
    catch (e) {
        if (e.code === 'ARG_UNKNOWN_OPTION') {
            console.log(safe_1.default.red(constants_1.invalidOption));
        }
        else {
            console.log(safe_1.default.red('エラーが発生しました'));
        }
        console.log(constants_1.listBooksHelpText);
        return null;
    }
}
var bookFormatters = {
    tsv: function (book) { return book.slug + (book.title ? '\t' + book.title : ''); },
    json: function (book) { return JSON.stringify(book); },
};
exports.exec = function (argv) {
    var args = parseArgs(argv);
    if (!args)
        return;
    if (args['--help']) {
        console.log(constants_1.listBooksHelpText);
        return;
    }
    var format = args['--format'] || 'tsv';
    if (!(format in bookFormatters)) {
        console.error("\u30A8\u30E9\u30FC\uFF1Aformat\u306E\u5024\uFF08" + format + "\uFF09\u304C\u4E0D\u6B63\u3067\u3059\u3002\"tsv\" \u307E\u305F\u306F \"json\" \u3092\u6307\u5B9A\u3057\u3066\u304F\u3060\u3055\u3044");
        process.exit(1);
    }
    var formatter = bookFormatters[format];
    var books = books_1.getAllBooks(['title']);
    var output = (books === null || books === void 0 ? void 0 : books.length) ? books.map(function (book) { return formatter(book); }).join('\n')
        : 'No books yet';
    console.log(output);
};
